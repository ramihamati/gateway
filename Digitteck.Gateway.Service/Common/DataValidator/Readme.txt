A custom xml validation that will check for dataannotations and custom validators inheriting ValidatorBase

A validator can be added using AddValidation method

If DataAnotations are used, decorate all the model classes with [Validatable]
